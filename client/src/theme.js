import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Customize the primary color
    },
    // Add more palette properties if desired
  },

  shape:{
    borderRadius:15
  }
  
});
